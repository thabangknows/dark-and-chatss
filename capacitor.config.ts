import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Dark_and_chatss',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
